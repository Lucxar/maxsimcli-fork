---
phase: 05-Parallel-Execution-Model
plan: 04
type: execute
wave: 2
depends_on: ["05-02"]
files_modified:
  - templates/workflows/execute-plan.md
  - packages/cli/src/core/verify.ts
  - templates/agents/executor.md
autonomous: true
requirements: [EXEC-05]
must_haves:
  truths:
    - "Pre-execution gate G1 validates all requirement IDs from PLAN frontmatter exist in REQUIREMENTS.md"
    - "Pre-execution gate G2 rejects plans whose requirements are already marked Complete"
    - "Post-execution gate G6 validates SUMMARY.md has evidence for each requirement in plan frontmatter"
    - "Executor agent documents requirement evidence in SUMMARY.md Requirement Evidence section"
    - "End-to-end traceability: REQUIREMENTS.md -> ROADMAP.md -> PLAN.md -> SUMMARY.md -> REQUIREMENTS.md"
  artifacts:
    - path: "templates/workflows/execute-plan.md"
      provides: "Pre-execution requirement gates G1 and G2, post-execution evidence gate G6"
      min_lines: 400
    - path: "packages/cli/src/core/verify.ts"
      provides: "validateRequirementExistence and validateRequirementStatus functions"
    - path: "templates/agents/executor.md"
      provides: "Instruction to populate Requirement Evidence section in SUMMARY"
  key_links:
    - from: "templates/workflows/execute-plan.md"
      to: "packages/cli/src/core/verify.ts"
      via: "CLI calls to verify requirement-existence and verify requirement-status"
      pattern: "maxsim-tools.cjs (verify requirement-existence|verify requirement-status)"
    - from: "templates/agents/executor.md"
      to: "templates/templates/summary.md"
      via: "Executor populates Requirement Evidence section"
      pattern: "Requirement Evidence"
---

<objective>
Enforce spec-driven development as a core methodology by adding pre-execution and post-execution gates. Before execution: validate requirement IDs exist in REQUIREMENTS.md (G1) and are not already Complete (G2). After execution: validate SUMMARY.md has evidence for each requirement (G6). Update the executor agent to document requirement evidence in SUMMARY.md. Add CLI verification functions for requirement gates.
</objective>

<context>
Read these files for implementation context:
- `templates/workflows/execute-plan.md` (add pre-execution gates before `execute` step, post-execution gate after `review_cycle`)
- `packages/cli/src/core/verify.ts` (add requirement validation functions)
- `packages/cli/src/cli.ts` (add CLI command routing for new verify functions)
- `templates/agents/executor.md` (add requirement evidence instruction)
- `.planning/phases/05-Parallel-Execution-Model/05-RESEARCH.md` (7 enforcement gates design)
- `.planning/REQUIREMENTS.md` (format reference for parsing)
</context>

<tasks>

<task id="1" type="auto">
<files>
- MODIFY: `packages/cli/src/core/verify.ts`
- MODIFY: `packages/cli/src/cli.ts`
</files>
<action>
**Step 1: Add requirement validation functions to verify.ts.**

Add two new exported functions:

1. `validateRequirementExistence(cwd: string, reqIds: string[]): Promise<{valid: boolean, missing: string[], found: string[]}>` — Reads REQUIREMENTS.md, parses all requirement IDs (pattern: `**{ID}**:`), checks that every ID in reqIds exists. Returns missing IDs.

2. `validateRequirementStatus(cwd: string, reqIds: string[]): Promise<{valid: boolean, already_complete: string[], pending: string[]}>` — Reads REQUIREMENTS.md, checks checkbox status for each reqId. IDs with `[x]` are already_complete. Returns list of already-complete IDs. Valid=true only if no IDs are already complete.

3. `validateEvidenceCompleteness(summaryPath: string, reqIds: string[]): Promise<{valid: boolean, missing_evidence: string[], has_evidence: string[]}>` — Reads a SUMMARY.md file, finds the `## Requirement Evidence` section, parses the table rows, checks that every reqId has a row with Status != "UNMET". Returns missing_evidence IDs.

**Step 2: Add CLI commands in cli.ts.**

Add command dispatches:

1. `'verify requirement-existence'` — Args: reqIds (space-separated). Calls `validateRequirementExistence()`. Returns JSON result.
2. `'verify requirement-status'` — Args: reqIds (space-separated). Calls `validateRequirementStatus()`. Returns JSON result.
3. `'verify evidence-completeness'` — Args: summaryPath, reqIds (space-separated). Calls `validateEvidenceCompleteness()`. Returns JSON result.

Follow existing verify.ts patterns (the file already has plan structure validation).
</action>
<verify>
<automated>cd C:/Development/cli/maxsim && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
</verify>
<done>
- verify.ts exports validateRequirementExistence, validateRequirementStatus, validateEvidenceCompleteness
- cli.ts routes verify requirement-existence, verify requirement-status, verify evidence-completeness
- TypeScript compiles without errors
</done>
</task>

<task id="2" type="auto">
<files>
- MODIFY: `templates/workflows/execute-plan.md`
- MODIFY: `templates/agents/executor.md`
</files>
<action>
**Step 1: Add pre-execution gates to execute-plan.md.**

Insert a new step `pre_execution_gates` between `load_prompt` and `execute` steps:

```xml
<step name="pre_execution_gates">
Validate requirements before execution. These gates ensure spec-driven development.

**Gate G1: Requirement Existence** — All requirement IDs from the plan's frontmatter must exist in REQUIREMENTS.md.

```bash
# Extract requirement IDs from plan frontmatter
REQ_IDS=$(grep "^requirements:" "$PLAN_PATH" | sed 's/requirements:\s*\[//;s/\]//;s/,/ /g;s/"//g;s/'\''//g' | tr -s ' ')
if [ -n "$REQ_IDS" ]; then
  G1_RESULT=$(node ~/.claude/maxsim/bin/maxsim-tools.cjs verify requirement-existence $REQ_IDS)
  G1_VALID=$(echo "$G1_RESULT" | node -e "process.stdin.on('data',d=>console.log(JSON.parse(d).valid))")
  if [ "$G1_VALID" != "true" ]; then
    G1_MISSING=$(echo "$G1_RESULT" | node -e "process.stdin.on('data',d=>console.log(JSON.parse(d).missing.join(', ')))")
    echo "GATE G1 FAILED: Requirements not found in REQUIREMENTS.md: $G1_MISSING"
    echo "Fix: Add missing requirements to REQUIREMENTS.md or update plan frontmatter."
    # STOP execution — this is a hard gate
  fi
fi
```

**Gate G2: Requirement Status** — Requirement IDs must not already be marked Complete.

```bash
if [ -n "$REQ_IDS" ]; then
  G2_RESULT=$(node ~/.claude/maxsim/bin/maxsim-tools.cjs verify requirement-status $REQ_IDS)
  G2_VALID=$(echo "$G2_RESULT" | node -e "process.stdin.on('data',d=>console.log(JSON.parse(d).valid))")
  if [ "$G2_VALID" != "true" ]; then
    G2_COMPLETE=$(echo "$G2_RESULT" | node -e "process.stdin.on('data',d=>console.log(JSON.parse(d).already_complete.join(', ')))")
    echo "GATE G2 WARNING: Requirements already marked complete: $G2_COMPLETE"
    echo "This plan may be re-implementing completed work. Proceeding with warning."
    # WARNING only — log but continue (re-execution scenarios are valid)
  fi
fi
```

**Step 2: Add post-execution evidence gate to execute-plan.md.**

Insert after the `review_cycle` step but before `git_commit_metadata`:

```xml
<step name="evidence_gate">
**Gate G6: Evidence Completeness** — SUMMARY.md must have evidence for each requirement.

```bash
if [ -n "$REQ_IDS" ]; then
  G6_RESULT=$(node ~/.claude/maxsim/bin/maxsim-tools.cjs verify evidence-completeness "$SUMMARY_PATH" $REQ_IDS)
  G6_VALID=$(echo "$G6_RESULT" | node -e "process.stdin.on('data',d=>console.log(JSON.parse(d).valid))")
  if [ "$G6_VALID" != "true" ]; then
    G6_MISSING=$(echo "$G6_RESULT" | node -e "process.stdin.on('data',d=>console.log(JSON.parse(d).missing_evidence.join(', ')))")
    echo "GATE G6: Missing requirement evidence for: $G6_MISSING"
    echo "Add Requirement Evidence rows to SUMMARY.md for these requirements before proceeding."
    # Present as warning with instruction to fix — executor should add evidence
  fi
fi
```
</step>
```

**Step 3: Update executor agent to document requirement evidence.**

In `templates/agents/executor.md`, add to the Execution Protocol section (after step 5 Commit):

```markdown
## Requirement Evidence

When creating SUMMARY.md, populate the `## Requirement Evidence` section:

1. Read the plan's `requirements` frontmatter field to get requirement IDs
2. For each requirement ID, document:
   - What was built that satisfies it (specific files, functions, behaviors)
   - How it can be verified (test command, manual check, or inspection)
   - Status: MET (fully satisfied), PARTIAL (needs more work), UNMET (not addressed)
3. Every requirement ID from the plan MUST have a row in the evidence table
```

Also add to the Completion Gate section:
```markdown
- Requirement Evidence section populated for all plan requirements (if `requirements` field exists in plan frontmatter)
```
</action>
<verify>
<automated>grep -c "pre_execution_gates\|evidence_gate\|Gate G1\|Gate G2\|Gate G6\|Requirement Evidence" C:/Development/cli/maxsim/templates/workflows/execute-plan.md C:/Development/cli/maxsim/templates/agents/executor.md</automated>
</verify>
<done>
- execute-plan.md has pre_execution_gates step with G1 (existence) and G2 (status) checks
- execute-plan.md has evidence_gate step with G6 (evidence completeness) check
- executor.md instructs agents to populate Requirement Evidence section in SUMMARY.md
- G1 is a hard gate (stops execution if requirements don't exist)
- G2 is a warning gate (logs but continues for re-execution scenarios)
- G6 is a warning gate (instructs fix but doesn't block)
</done>
</task>

</tasks>

<verification>
```bash
# Verify CLI functions
grep -n "validateRequirementExistence\|validateRequirementStatus\|validateEvidenceCompleteness" C:/Development/cli/maxsim/packages/cli/src/core/verify.ts

# Verify pre-execution gates
grep -n "Gate G1\|Gate G2\|pre_execution_gates" C:/Development/cli/maxsim/templates/workflows/execute-plan.md

# Verify post-execution gate
grep -n "Gate G6\|evidence_gate\|evidence-completeness" C:/Development/cli/maxsim/templates/workflows/execute-plan.md

# Verify executor agent update
grep -n "Requirement Evidence" C:/Development/cli/maxsim/templates/agents/executor.md

# TypeScript compilation
cd C:/Development/cli/maxsim && npx tsc --noEmit --pretty 2>&1 | tail -5
```
</verification>

<success_criteria>
- [ ] verify.ts has 3 new requirement validation functions
- [ ] cli.ts routes 3 new verify commands
- [ ] execute-plan.md has pre_execution_gates step with G1 and G2 gates
- [ ] execute-plan.md has evidence_gate step with G6 gate
- [ ] executor.md instructs agents to populate Requirement Evidence section
- [ ] G1 is a hard gate, G2 and G6 are warning gates
- [ ] TypeScript compiles without errors
</success_criteria>

<output>
On completion, update STATE.md and ROADMAP.md. Commit:
```
feat(05-04): add spec-driven enforcement gates and requirement evidence tracking
```
</output>
