---
phase: 03-Command-Surface-Simplification
plan: 05
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
  - 03-03
  - 03-04
files_modified:
  - templates/commands/maxsim/quick.md
  - templates/workflows/quick.md
  - templates/commands/maxsim/progress.md
  - templates/workflows/progress.md
  - templates/commands/maxsim/settings.md
  - templates/workflows/settings.md
autonomous: true
requirements:
  - CMD-04
  - CMD-07
  - CMD-08
must_haves:
  truths:
    - "/maxsim:quick supports todo capture -- 'save for later' creates a GitHub Issue with 'todo' label"
    - "/maxsim:progress shows GitHub Issues-based status overview and offers milestone completion when all phases done"
    - "/maxsim:settings absorbs set-profile functionality"
  artifacts:
    - path: "templates/commands/maxsim/quick.md"
      provides: "Enhanced quick command with todo capture"
      min_lines: 20
    - path: "templates/workflows/quick.md"
      provides: "Enhanced quick workflow with todo save-for-later and triage"
      min_lines: 100
    - path: "templates/commands/maxsim/progress.md"
      provides: "Enhanced progress command"
      min_lines: 15
    - path: "templates/workflows/progress.md"
      provides: "Enhanced progress workflow with GitHub Issues and milestone completion"
      min_lines: 80
    - path: "templates/commands/maxsim/settings.md"
      provides: "Enhanced settings command absorbing set-profile"
      min_lines: 15
    - path: "templates/workflows/settings.md"
      provides: "Enhanced settings workflow with profile management"
      min_lines: 50
  key_links:
    - from: "templates/workflows/quick.md"
      to: "MCP todo tools"
      via: "mcp_add_todo for save-for-later"
      pattern: "mcp_add_todo\|save.*later\|todo.*label"
    - from: "templates/workflows/progress.md"
      to: "MCP state tools"
      via: "mcp_get_all_progress for GitHub Issues status"
      pattern: "mcp_get_all_progress"
---

<objective>
Enhance the three kept commands (`/maxsim:quick`, `/maxsim:progress`, `/maxsim:settings`) to absorb capabilities from removed commands.

After this plan:
1. `/maxsim:quick` supports "save for later" todo capture (from `/maxsim:add-todo` and `/maxsim:check-todos`)
2. `/maxsim:progress` shows GitHub Issues-based progress and offers milestone completion (from `/maxsim:audit-milestone` and `/maxsim:complete-milestone`)
3. `/maxsim:settings` absorbs profile management (from `/maxsim:set-profile`)
4. All three commands reference only new command names, not old ones
</objective>

<execution_context>
This is Phase 3, Plan 05, Wave 3. Depends on Plans 01-04 (all new commands must exist before enhancing kept commands, since "next step" suggestions and routing must reference the new command names).

Capability migration from CONTEXT.md:
- add-todo, check-todos -> fold into /maxsim:quick ("save for later" creates GitHub Issue with 'todo' label)
- audit-milestone, complete-milestone, plan-milestone-gaps -> fold into /maxsim:progress
- set-profile -> fold into /maxsim:settings

Existing files being enhanced (READ before modifying):
- `templates/commands/maxsim/quick.md` (42 lines)
- `templates/workflows/quick.md` (570 lines, 17KB)
- `templates/commands/maxsim/progress.md` (23 lines)
- `templates/workflows/progress.md` (389 lines, 10KB)
- `templates/commands/maxsim/settings.md` (37 lines)
- `templates/workflows/settings.md` (218 lines, 8KB)

Existing todo workflows to absorb:
- `templates/workflows/add-todo.md` (247 lines, 7KB)
- `templates/workflows/check-todos.md` (261 lines, 8KB)

Existing milestone workflows to reference:
- `templates/workflows/complete-milestone.md` (existing, referenced by progress workflow)
</execution_context>

<context>
@templates/commands/maxsim/quick.md (being enhanced)
@templates/workflows/quick.md (being enhanced)
@templates/workflows/add-todo.md (todo logic to absorb)
@templates/workflows/check-todos.md (todo logic to absorb)
@templates/commands/maxsim/progress.md (being enhanced)
@templates/workflows/progress.md (being enhanced)
@templates/commands/maxsim/settings.md (being enhanced)
@templates/workflows/settings.md (being enhanced)
@templates/commands/maxsim/set-profile.md (being absorbed)
@.planning/phases/03-Command-Surface-Simplification/03-CONTEXT.md (locked decisions)
</context>

<tasks>

<task id="01" type="auto">
<files>
- templates/commands/maxsim/quick.md (MODIFY)
- templates/workflows/quick.md (MODIFY)
</files>
<action>
Read the existing files first, then enhance.

**Enhance the quick command template:**
Update the description to mention todo capture. Add to the objective: "Also supports 'save for later' -- capturing ideas as GitHub Issues with 'todo' label for future work."

Add to argument-hint: `"[--full] [--todo]"` where `--todo` triggers todo management mode.

**Enhance the quick workflow:**
Read the existing `templates/workflows/quick.md` (17KB) and add a new section for todo management. Keep all existing quick task logic intact.

Add a new mode detection at the top of the workflow:

```markdown
## 0. Mode Detection

If `--todo` flag is present OR user's description starts with "todo", "save", "remember", "later":
  -> Enter Todo Mode (Section X below)
Else:
  -> Enter Quick Task Mode (existing workflow)
```

Add a new Todo Mode section (adapted from `add-todo.md` and `check-todos.md` workflows):

```markdown
## Todo Mode

### List Existing Todos
```bash
node .claude/maxsim/bin/maxsim-tools.cjs todos list
```

Display current todos in a clean table.

### Capture New Todo
If user provided a description:
1. Parse priority from description (HIGH/MEDIUM/LOW, default MEDIUM)
2. Create todo via `node .claude/maxsim/bin/maxsim-tools.cjs todos add "$DESCRIPTION" "$PRIORITY"`
3. Best-effort: create GitHub Issue with 'todo' label via `mcp_add_todo`
4. Confirm: "Saved: {description} (priority: {priority})"

### Complete Todo
If user references an existing todo to complete:
1. Complete via `node .claude/maxsim/bin/maxsim-tools.cjs todos complete "$ID"`
2. Best-effort: close corresponding GitHub Issue
3. Confirm: "Completed: {description}"

### Triage
If user says "what should I work on?" or similar:
1. List all pending todos sorted by priority
2. Show context: which todos relate to current phase, which are standalone
3. Suggest: "Pick a todo to work on, or describe a new task"
```

Update all "next step" suggestions in the workflow to reference new command names (not old ones). Replace any mentions of `/maxsim:add-todo`, `/maxsim:check-todos`, `/maxsim:plan-phase`, `/maxsim:execute-phase` with their new equivalents.

Keep the workflow under 22KB total.
</action>
<verify>
<automated>bash -c "test -f templates/commands/maxsim/quick.md && test -f templates/workflows/quick.md && echo 'FILES_EXIST' || echo 'MISSING'"</automated>
<automated>bash -c "grep -q 'todo\|Todo\|save.*later' templates/workflows/quick.md && echo 'TODO_MODE_OK' || echo 'NO_TODO_MODE'"</automated>
<automated>bash -c "grep -l 'maxsim:add-todo\|maxsim:check-todos\|maxsim:plan-phase\|maxsim:execute-phase' templates/workflows/quick.md && echo 'OLD_REFS_FOUND' || echo 'NO_OLD_REFS_OK'"</automated>
<automated>bash -c "wc -c < templates/workflows/quick.md | awk '{if ($1 < 22528) print \"SIZE_OK: \" $1 \" bytes\"; else print \"TOO_LARGE: \" $1 \" bytes\"}'"</automated>
</verify>
<done>
- quick.md command template updated with --todo flag
- quick.md workflow enhanced with todo management mode
- Todo capture creates local todo + best-effort GitHub Issue
- Triage mode helps user pick next todo
- No old command references
- Total workflow size under 22KB
</done>
</task>

<task id="02" type="auto">
<files>
- templates/commands/maxsim/progress.md (MODIFY)
- templates/workflows/progress.md (MODIFY)
- templates/commands/maxsim/settings.md (MODIFY)
- templates/workflows/settings.md (MODIFY)
</files>
<action>
Read all four existing files first, then enhance.

**Enhance progress command + workflow:**

Update the progress command template description to include milestone management: "Check project progress, milestone status, and offer milestone completion when all phases are done."

Update allowed-tools to include Task (for spawning milestone completion agent if needed).

Enhance the progress workflow by adding:

1. **GitHub Issues progress** -- Use MCP tool `mcp_get_all_progress` to get phase progress from GitHub Issues. Display alongside local ROADMAP.md progress for cross-validation.

2. **Milestone completion offer** -- After showing progress, if ALL phases are complete:
   ```markdown
   ## Milestone Complete!

   All {N} phases are complete. Ready to wrap up?

   1. Complete milestone (archive, create release notes)
   2. Start new milestone
   3. Just show me the progress
   ```
   If user chooses option 1: delegate to `@./workflows/new-milestone.md` completion flow (or inline a simplified version).
   If user chooses option 2: delegate to `@./workflows/new-milestone.md` creation flow.

3. **Phase gap detection** -- If some phases have issues (failed verification, stale plans), highlight them:
   ```markdown
   ## Issues Detected

   - Phase 3: Verification failed (2 truths unmet)
   - Phase 5: Plan exists but not executed
   ```

Update all routing suggestions to use new command names. Replace `/maxsim:plan-phase` with `/maxsim:plan`, `/maxsim:execute-phase` with `/maxsim:execute`, etc.

Keep the workflow under 14KB.

**Enhance settings command + workflow:**

Update the settings command template description to include profile management.

Enhance the settings workflow by adding:

1. **Profile management** -- Absorb `/maxsim:set-profile` functionality. Add a 6th question to the interactive prompt:
   ```
   6. Model profile: quality (best models) / balanced (mix) / budget (cost-effective)
      Current: {current_profile}
   ```

2. **Profile description** -- Show what each profile means in terms of actual model assignments (from MODEL_PROFILES in core.ts).

3. Update the confirmation display to show the profile selection.

Replace any old command references in the workflow.

Keep the workflow under 10KB.
</action>
<verify>
<automated>bash -c "grep -q 'mcp_get_all_progress\|GitHub.*progress\|github.*progress' templates/workflows/progress.md && echo 'GITHUB_PROGRESS_OK' || echo 'NO_GITHUB_PROGRESS'"</automated>
<automated>bash -c "grep -q 'milestone.*complete\|Milestone.*Complete\|complete.*milestone' templates/workflows/progress.md && echo 'MILESTONE_COMPLETION_OK' || echo 'NO_MILESTONE_COMPLETION'"</automated>
<automated>bash -c "grep -q 'profile\|Profile\|model_profile' templates/workflows/settings.md && echo 'PROFILE_MGMT_OK' || echo 'NO_PROFILE_MGMT'"</automated>
<automated>bash -c "grep -rl 'maxsim:plan-phase\|maxsim:execute-phase\|maxsim:set-profile\|maxsim:audit-milestone\|maxsim:complete-milestone' templates/workflows/progress.md templates/workflows/settings.md && echo 'OLD_REFS_FOUND' || echo 'NO_OLD_REFS_OK'"</automated>
</verify>
<done>
- progress.md enhanced with GitHub Issues progress (mcp_get_all_progress)
- progress.md enhanced with milestone completion offer when all phases done
- progress.md enhanced with phase gap detection
- settings.md enhanced with profile management (absorbed set-profile)
- No old command references in any modified file
- progress workflow under 14KB, settings workflow under 10KB
</done>
</task>

</tasks>

<verification>
1. Quick workflow has todo mode: `grep 'Todo Mode\|todo.*mode\|--todo' templates/workflows/quick.md`
2. Progress uses GitHub: `grep 'mcp_get_all_progress' templates/workflows/progress.md`
3. Progress offers milestone completion: `grep -i 'milestone.*complete' templates/workflows/progress.md`
4. Settings has profile: `grep 'profile\|model_profile' templates/workflows/settings.md`
5. No old command refs in any modified file
</verification>

<success_criteria>
- [ ] /maxsim:quick enhanced with todo capture mode
- [ ] /maxsim:progress enhanced with GitHub Issues progress
- [ ] /maxsim:progress offers milestone completion when all phases done
- [ ] /maxsim:settings absorbs set-profile functionality
- [ ] All "next step" suggestions updated to new command names
- [ ] No old command references in any modified file
- [ ] All enhanced workflows within size limits
</success_criteria>

<output>
Commit all modified files.
</output>
