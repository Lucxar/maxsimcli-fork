---
phase: 03-Command-Surface-Simplification
plan: 06
type: execute
wave: 4
depends_on:
  - 03-01
  - 03-02
  - 03-03
  - 03-04
  - 03-05
files_modified:
  - templates/commands/maxsim/add-phase.md
  - templates/commands/maxsim/add-tests.md
  - templates/commands/maxsim/add-todo.md
  - templates/commands/maxsim/artefakte.md
  - templates/commands/maxsim/audit-milestone.md
  - templates/commands/maxsim/batch.md
  - templates/commands/maxsim/check-drift.md
  - templates/commands/maxsim/check-todos.md
  - templates/commands/maxsim/cleanup.md
  - templates/commands/maxsim/complete-milestone.md
  - templates/commands/maxsim/discuss-phase.md
  - templates/commands/maxsim/discuss.md
  - templates/commands/maxsim/execute-phase.md
  - templates/commands/maxsim/health.md
  - templates/commands/maxsim/init-existing.md
  - templates/commands/maxsim/insert-phase.md
  - templates/commands/maxsim/list-phase-assumptions.md
  - templates/commands/maxsim/map-codebase.md
  - templates/commands/maxsim/new-milestone.md
  - templates/commands/maxsim/new-project.md
  - templates/commands/maxsim/pause-work.md
  - templates/commands/maxsim/plan-milestone-gaps.md
  - templates/commands/maxsim/plan-phase.md
  - templates/commands/maxsim/realign.md
  - templates/commands/maxsim/reapply-patches.md
  - templates/commands/maxsim/remove-phase.md
  - templates/commands/maxsim/research-phase.md
  - templates/commands/maxsim/resume-work.md
  - templates/commands/maxsim/roadmap.md
  - templates/commands/maxsim/sdd.md
  - templates/commands/maxsim/set-profile.md
  - templates/commands/maxsim/update.md
  - templates/commands/maxsim/verify-work.md
autonomous: true
requirements:
  - CMD-09
must_haves:
  truths:
    - "All ~29 old command template files are deleted from templates/commands/maxsim/"
    - "Only 9 command files remain: init.md, plan.md, execute.md, go.md, progress.md, debug.md, quick.md, settings.md, help.md"
    - "Obsolete workflow files that are no longer referenced by any command are deleted"
  artifacts:
    - path: "templates/commands/maxsim/"
      provides: "Directory with exactly 9 command files"
  key_links: []
---

<objective>
Delete all ~29 old command template files and obsolete workflow files. This is the clean break -- no redirects, no aliases, no wrappers.

After this plan:
1. Only 9 command files remain in `templates/commands/maxsim/`
2. Obsolete workflow files are deleted (those only used by removed commands)
3. Workflow files still used by the new commands or by agents are kept
</objective>

<execution_context>
This is Phase 3, Plan 06, Wave 4. Depends on ALL previous plans (must be certain the new commands + workflows exist before deleting old ones).

Key constraint from CONTEXT.md: "~29 old commands deleted -- clean break, no redirects, no aliases"

CRITICAL: Before deleting anything, verify the new command files exist:
```bash
for f in init.md plan.md execute.md go.md progress.md debug.md quick.md settings.md help.md; do
  test -f "templates/commands/maxsim/$f" || echo "MISSING: $f"
done
```

If ANY new command file is missing, STOP and report the error.
</execution_context>

<context>
@.planning/phases/03-Command-Surface-Simplification/03-CONTEXT.md (deletion list from "Commands Being Removed" section)
@.planning/phases/03-Command-Surface-Simplification/03-RESEARCH.md (workflow fate table)
</context>

<tasks>

<task id="01" type="auto">
<files>
- templates/commands/maxsim/add-phase.md (DELETE)
- templates/commands/maxsim/add-tests.md (DELETE)
- templates/commands/maxsim/add-todo.md (DELETE)
- templates/commands/maxsim/artefakte.md (DELETE)
- templates/commands/maxsim/audit-milestone.md (DELETE)
- templates/commands/maxsim/batch.md (DELETE)
- templates/commands/maxsim/check-drift.md (DELETE)
- templates/commands/maxsim/check-todos.md (DELETE)
- templates/commands/maxsim/cleanup.md (DELETE)
- templates/commands/maxsim/complete-milestone.md (DELETE)
- templates/commands/maxsim/discuss-phase.md (DELETE)
- templates/commands/maxsim/discuss.md (DELETE)
- templates/commands/maxsim/execute-phase.md (DELETE)
- templates/commands/maxsim/health.md (DELETE)
- templates/commands/maxsim/init-existing.md (DELETE)
- templates/commands/maxsim/insert-phase.md (DELETE)
- templates/commands/maxsim/list-phase-assumptions.md (DELETE)
- templates/commands/maxsim/map-codebase.md (DELETE)
- templates/commands/maxsim/new-milestone.md (DELETE)
- templates/commands/maxsim/new-project.md (DELETE)
- templates/commands/maxsim/pause-work.md (DELETE)
- templates/commands/maxsim/plan-milestone-gaps.md (DELETE)
- templates/commands/maxsim/plan-phase.md (DELETE)
- templates/commands/maxsim/realign.md (DELETE)
- templates/commands/maxsim/reapply-patches.md (DELETE)
- templates/commands/maxsim/remove-phase.md (DELETE)
- templates/commands/maxsim/research-phase.md (DELETE)
- templates/commands/maxsim/resume-work.md (DELETE)
- templates/commands/maxsim/roadmap.md (DELETE)
- templates/commands/maxsim/sdd.md (DELETE)
- templates/commands/maxsim/set-profile.md (DELETE)
- templates/commands/maxsim/update.md (DELETE)
- templates/commands/maxsim/verify-work.md (DELETE)
</files>
<action>
**Step 1: Pre-flight check** -- Verify ALL 9 new command files exist before deleting anything.

```bash
MISSING=""
for f in init.md plan.md execute.md go.md progress.md debug.md quick.md settings.md help.md; do
  test -f "templates/commands/maxsim/$f" || MISSING="$MISSING $f"
done
if [ -n "$MISSING" ]; then
  echo "ABORT: Missing new command files:$MISSING"
  exit 1
fi
echo "All 9 new commands verified. Proceeding with deletion."
```

If ANY are missing: STOP. Do NOT delete old commands. Report which new files are missing.

**Step 2: Delete old command files.**

Delete the following 29 files (note: some may already have been replaced by new versions -- only delete the OLD ones listed):

```bash
cd templates/commands/maxsim/
rm -f add-phase.md add-tests.md add-todo.md artefakte.md audit-milestone.md
rm -f batch.md check-drift.md check-todos.md cleanup.md complete-milestone.md
rm -f discuss-phase.md discuss.md execute-phase.md health.md init-existing.md
rm -f insert-phase.md list-phase-assumptions.md map-codebase.md new-milestone.md
rm -f new-project.md pause-work.md plan-milestone-gaps.md plan-phase.md
rm -f realign.md reapply-patches.md remove-phase.md research-phase.md
rm -f resume-work.md roadmap.md sdd.md set-profile.md update.md verify-work.md
```

**Step 3: Post-deletion verification** -- Confirm exactly 9 files remain.

```bash
ls templates/commands/maxsim/*.md | sort
# Expected: debug.md execute.md go.md help.md init.md plan.md progress.md quick.md settings.md
count=$(ls templates/commands/maxsim/*.md | wc -l)
echo "Remaining command files: $count (expected: 9)"
```

**Step 4: Delete obsolete workflow files.**

Delete workflow files that are ONLY used by removed commands and have no other consumers:

```bash
cd templates/workflows/
# Workflows absorbed into new commands (logic is in new sub-workflows or inlined):
rm -f add-phase.md          # -> MCP tool only
rm -f add-tests.md          # -> deleted (unused)
rm -f add-todo.md           # -> absorbed into quick.md workflow
rm -f audit-milestone.md    # -> absorbed into progress.md workflow
rm -f check-drift.md        # -> agent skill only
rm -f check-todos.md        # -> absorbed into check-todos.md workflow
rm -f cleanup.md            # -> deleted (unused)
rm -f complete-milestone.md # -> absorbed into progress.md workflow
rm -f discuss.md            # -> absorbed into quick.md triage
rm -f insert-phase.md       # -> MCP tool only
rm -f list-phase-assumptions.md # -> deleted (unused)
rm -f map-codebase.md       # -> agent skill only
rm -f pause-work.md         # -> auto-checkpoint in all commands
rm -f plan-milestone-gaps.md # -> absorbed into progress.md
rm -f realign.md            # -> agent skill only
rm -f remove-phase.md       # -> MCP tool only
rm -f resume-project.md     # -> auto-resume in all commands
rm -f roadmap.md            # -> MCP tool only
rm -f set-profile.md        # -> absorbed into settings.md workflow
rm -f transition.md         # -> deleted (unused)
rm -f update.md             # -> handled by installer
```

KEEP these workflow files (they are still referenced):
- `discuss-phase.md` -- referenced by `plan-discuss.md` (adapted content, but may still be @referenced)
- `plan-phase.md` -- referenced by `plan-create.md` (adapted content)
- `execute-phase.md` -- referenced by `execute.md` (adapted content)
- `execute-plan.md` -- directly used for per-plan subagent execution
- `new-project.md` -- delegated to by init.md workflow
- `init-existing.md` -- delegated to by init.md workflow
- `new-milestone.md` -- delegated to by init.md and progress.md workflows
- `verify-work.md` -- logic absorbed into execute.md but may be @referenced
- `verify-phase.md` -- used by execute.md for verifier agent spawning
- `research-phase.md` -- referenced by plan-research.md
- `help.md` -- rewritten (new version)
- `quick.md` -- enhanced (new version)
- `progress.md` -- enhanced (new version)
- `settings.md` -- enhanced (new version)
- `plan.md` -- new (created in Plan 01)
- `plan-discuss.md` -- new (created in Plan 01)
- `plan-research.md` -- new (created in Plan 01)
- `plan-create.md` -- new (created in Plan 01)
- `init.md` -- new (created in Plan 02)
- `execute.md` -- new (created in Plan 03)
- `go.md` -- new (created in Plan 04)
- `batch.md` -- kept as executor skill (13KB)
- `sdd.md` -- kept as executor skill (14KB)
- `diagnose-issues.md` -- used by debug workflow
- `discovery-phase.md` -- used by plan workflow
- `health.md` -- used by verifier agent

IMPORTANT: Before deleting any workflow file, verify it is NOT referenced by any remaining command or workflow file:
```bash
grep -rl "filename-without-extension" templates/commands/ templates/workflows/ templates/agents/ 2>/dev/null
```

If a file IS referenced, do NOT delete it. Note it as "kept -- still referenced by {file}".
</action>
<verify>
<automated>bash -c "count=$(ls templates/commands/maxsim/*.md 2>/dev/null | wc -l); echo \"Command files: $count (expected: 9)\"; if [ $count -eq 9 ]; then echo 'COUNT_OK'; else echo 'COUNT_WRONG'; fi"</automated>
<automated>bash -c "ls templates/commands/maxsim/*.md 2>/dev/null | sort | while read f; do basename $f; done"</automated>
<automated>bash -c "for old in add-phase add-tests add-todo artefakte audit-milestone batch check-drift check-todos cleanup complete-milestone discuss-phase discuss execute-phase health init-existing insert-phase list-phase-assumptions map-codebase new-milestone new-project pause-work plan-milestone-gaps plan-phase realign reapply-patches remove-phase research-phase resume-work roadmap sdd set-profile update verify-work; do test -f templates/commands/maxsim/$old.md && echo \"NOT_DELETED: $old.md\"; done; echo 'DELETION_CHECK_DONE'"</automated>
</verify>
<done>
- All ~29 old command files deleted from templates/commands/maxsim/
- Exactly 9 command files remain: init, plan, execute, go, progress, debug, quick, settings, help
- Obsolete workflow files deleted (only those with no remaining consumers)
- Pre-flight check verified all new commands exist before deletion
- Post-deletion verification confirmed correct file count
</done>
</task>

</tasks>

<verification>
1. Exactly 9 command files: `ls templates/commands/maxsim/*.md | wc -l` = 9
2. Correct 9 files: debug.md, execute.md, go.md, help.md, init.md, plan.md, progress.md, quick.md, settings.md
3. No old command files remain
4. Remaining workflow files are all referenced by at least one command or agent
</verification>

<success_criteria>
- [ ] Pre-flight check passed (all 9 new commands verified before deletion)
- [ ] All ~29 old command files deleted
- [ ] Exactly 9 command files remain
- [ ] Obsolete workflow files deleted
- [ ] No workflow files deleted that are still referenced
</success_criteria>

<output>
Commit deletions with clear message listing what was removed.
</output>
