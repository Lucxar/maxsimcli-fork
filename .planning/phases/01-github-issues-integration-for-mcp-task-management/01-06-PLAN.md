---
phase: 01-github-issues-integration-for-mcp-task-management
plan: 06
type: execute
wave: 5
depends_on:
  - 01-05
files_modified:
  - packages/cli/src/mcp/github-tools.ts
  - packages/cli/src/mcp/board-tools.ts
  - packages/cli/src/mcp/phase-tools.ts
  - packages/cli/src/mcp/todo-tools.ts
  - packages/cli/src/mcp/state-tools.ts
autonomous: false
requirements:
  - AC-01
  - AC-02
  - AC-03
  - AC-04
  - AC-05
  - AC-06
  - AC-07
  - AC-08
  - AC-09
  - AC-10
  - AC-11
  - AC-12
  - AC-13
  - AC-14
  - AC-15
  - AC-16
  - AC-17
must_haves:
  truths:
    - "npm run build succeeds without errors"
    - "All MCP tools compile and register without runtime errors"
    - "Project board creation works end-to-end via gh CLI"
    - "Issue creation with labels and project assignment works"
    - "Graceful degradation works when gh not authenticated"
  artifacts:
    - path: "packages/cli/dist/mcp-server.cjs"
      provides: "Built MCP server with all GitHub tools registered"
      min_lines: 1
  key_links:
    - from: "packages/cli/dist/mcp-server.cjs"
      to: "packages/cli/src/mcp/index.ts"
      via: "tsdown build produces bundled MCP server"
      pattern: "registerAllTools"
---

<objective>
Build verification and end-to-end smoke test. Run `npm run build` to verify the full project compiles. Then perform a manual verification checkpoint where the user confirms GitHub integration works against a real repository.
</objective>

<execution_context>
All code has been written in Plans 01-05. This plan verifies everything compiles, builds, and works end-to-end.
</execution_context>

<context>
@packages/cli/src/mcp/index.ts (tool registration entry point)
@packages/cli/src/github/index.ts (github module barrel export)
@CLAUDE.md (build commands: npm run build)
</context>

<tasks>

<task id="6.1" type="auto">
<files>
packages/cli/src/mcp/github-tools.ts
packages/cli/src/mcp/board-tools.ts
packages/cli/src/mcp/phase-tools.ts
packages/cli/src/mcp/todo-tools.ts
packages/cli/src/mcp/state-tools.ts
packages/cli/src/github/types.ts
packages/cli/src/github/gh.ts
packages/cli/src/github/mapping.ts
packages/cli/src/github/issues.ts
packages/cli/src/github/projects.ts
packages/cli/src/github/labels.ts
packages/cli/src/github/milestones.ts
packages/cli/src/github/templates.ts
packages/cli/src/github/sync.ts
packages/cli/src/github/index.ts
</files>
<action>
Run the full build pipeline and fix any compilation or bundling errors.

1. Run `npx tsc --noEmit` from packages/cli to type-check all source files. Fix any type errors.
2. Run `npm run build` from the monorepo root. This runs tsdown to bundle everything.
3. If build fails, diagnose and fix:
   - Import path issues (must use `.js` extension in imports for ESM compat with tsdown)
   - Missing exports from barrel files
   - Type mismatches between github modules and mcp tools
   - Circular dependency issues
4. Run `npm run lint` and fix any Biome linting errors in the new files.
5. Verify the built MCP server includes GitHub tools: check that `dist/mcp-server.cjs` exists and is larger than before (contains new tool code).

Common issues to watch for:
- tsdown may need the `packages/cli/src/github/` directory added to its entry points or include paths
- Import paths must use `.js` extension (e.g., `from './gh.js'` not `from './gh'`)
- The `child_process` module should be external in tsdown config
- `escapeStringRegexp` in state-tools.ts is an ESM-only package — verify it still works after modifications

After fixes, run build again to confirm clean build.
</action>
<verify>
<automated>cd /c/Development/cli/maxsim && npm run build 2>&1 | tail -20</automated>
</verify>
<done>
- `npx tsc --noEmit` passes with 0 errors
- `npm run build` succeeds
- `npm run lint` passes or only has pre-existing warnings
- dist/mcp-server.cjs exists and includes GitHub tool registrations
</done>
</task>

<task id="6.2" type="checkpoint:human-verify" gate="blocking">
<what-built>
Complete GitHub Issues integration for MAXSIM MCP tools:
- 8 new modules in packages/cli/src/github/ (types, gh wrapper, mapping, issues, projects, labels, milestones, templates)
- 2 new MCP tool files (github-tools.ts with 10 tools, board-tools.ts with 4 tools)
- 3 modified MCP tool files (phase-tools.ts, todo-tools.ts, state-tools.ts with GitHub integration)
- 1 new tool in phase-tools.ts (mcp_bounce_issue)
- Total: 15 new MCP tools + GitHub hooks in existing tools
</what-built>
<how-to-verify>
**Step 1: Build health (required)**
Run `npm run build` from repo root. Confirm zero errors.

**Step 2: Tool registration count (required)**
Run: `grep -c "server.tool(" packages/cli/src/mcp/github-tools.ts packages/cli/src/mcp/board-tools.ts`
- github-tools.ts should show 10 (setup, create plan issues, create todo, move, close, comment, import, sync, supersede, create PR)
- board-tools.ts should show 4 (query board, search issues, get detail, set estimate)
- Total: 14 new tool registrations plus 1 mcp_bounce_issue in phase-tools.ts = 15 new tools

**Step 3: Type definitions match schema (required)**
Run: `grep "export" packages/cli/src/github/types.ts`
Verify the following exports exist: GhResult, AuthStatus, GitHubMode, IssueMappingFile, PhaseMapping, TaskIssueMapping, IssueStatus, MAXSIM_LABELS, FIBONACCI_POINTS, DEFAULT_STATUS_OPTIONS (10 exports matching RESEARCH.md schema)

**Step 4: Issue template YAML validity (required)**
Run: `grep -A 2 "phase-task.yml\|todo.yml" packages/cli/src/github/templates.ts`
Verify template strings contain valid GitHub Issue Forms YAML: `name:`, `labels:`, `body:`, `type: textarea`

**Step 5: AC-08 wiring -- buildPrBody is callable (required)**
Run: `grep "buildPrBody" packages/cli/src/mcp/github-tools.ts packages/cli/src/github/issues.ts`
- issues.ts should define and export buildPrBody
- github-tools.ts should import and call buildPrBody in the mcp_create_pr tool handler
- This confirms AC-08 (PRs auto-link via Closes #N) is wired end-to-end, not orphaned

**Step 6: Graceful degradation (AC-17) -- structural check (required)**
Run: `grep -c "detectGitHubMode" packages/cli/src/mcp/github-tools.ts`
Should be >= 10 (one per tool). Every GitHub MCP tool must check mode before calling gh.

Then test actual degradation by temporarily unauthing:
```bash
# If you can temporarily test (optional -- structural check above is sufficient):
# gh auth logout (or rename ~/.config/gh/hosts.yml)
# echo '{}' | timeout 5 node packages/cli/dist/mcp-server.cjs
# Verify it starts without crash (graceful degradation = no exceptions on missing auth)
# gh auth login (restore auth)
```

**Step 7: Key file existence and minimum size (required)**
Run:
```bash
wc -l packages/cli/src/github/types.ts packages/cli/src/github/gh.ts packages/cli/src/github/mapping.ts packages/cli/src/github/issues.ts packages/cli/src/github/projects.ts packages/cli/src/github/labels.ts packages/cli/src/github/milestones.ts packages/cli/src/github/templates.ts packages/cli/src/github/sync.ts packages/cli/src/github/index.ts packages/cli/src/mcp/github-tools.ts packages/cli/src/mcp/board-tools.ts
```
Minimum lines: types.ts >= 80, gh.ts >= 120, mapping.ts >= 80, issues.ts >= 200, projects.ts >= 120, labels.ts >= 30, milestones.ts >= 60, templates.ts >= 50, sync.ts >= 80, github-tools.ts >= 200, board-tools.ts >= 150

**Step 8: No forbidden patterns (required)**
Run:
```bash
grep -rn "process.exit\|octokit\|@octokit\|node-fetch" packages/cli/src/github/ packages/cli/src/mcp/github-tools.ts packages/cli/src/mcp/board-tools.ts
```
Should return zero results. We use gh CLI exclusively, never octokit/node-fetch, and never process.exit().

**Step 9: Optional live smoke test (if gh CLI is authenticated)**
If you have gh CLI authenticated with project scope:
1. Run `gh auth status` -- confirm authenticated with project scope
2. Create a test project: `gh project create --owner @me --title "MAXSIM-test-delete-me"`
3. Create a test issue: `gh issue create --title "[TEST] Smoke test" --label maxsim --body "Test issue"`
4. Verify the issue appears: `gh issue list --label maxsim`
5. Clean up: close the issue and delete the test project
</how-to-verify>
<resume-signal>Type "approved" to confirm verification passes, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` exits with code 0
2. `npx tsc --noEmit` in packages/cli exits with code 0
3. `npm run lint` passes
4. dist/mcp-server.cjs file size is larger than before (contains new GitHub tool code)
5. 15 new MCP tool registrations confirmed
6. types.ts exports match RESEARCH.md schema (10 exports)
7. buildPrBody wired into mcp_create_pr (AC-08 not orphaned)
8. detectGitHubMode called in every GitHub tool (AC-17 graceful degradation)
9. No forbidden imports (octokit, node-fetch, process.exit)
10. Manual verification of MCP server startup (checkpoint)
</verification>

<success_criteria>
- Full monorepo build passes (npm run build)
- TypeScript compilation passes (npx tsc --noEmit)
- Linting passes (npm run lint)
- 15 new MCP tools registered and verified
- AC-08 (buildPrBody) wired end-to-end via mcp_create_pr
- AC-17 (graceful degradation) structurally verified via detectGitHubMode grep
- No forbidden patterns (octokit, process.exit, node-fetch)
- All github module files meet minimum line counts
- Issue template YAML is valid
- MCP server starts without runtime errors
- User confirms build and verification checks pass
</success_criteria>

<output>
Commit message: "feat(github): build verification and compilation fixes for GitHub integration"
Files: any files that needed fixing during build verification
</output>
