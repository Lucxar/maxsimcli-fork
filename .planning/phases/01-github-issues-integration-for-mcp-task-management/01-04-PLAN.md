---
phase: 01-github-issues-integration-for-mcp-task-management
plan: 04
type: execute
wave: 3
depends_on:
  - 01-02
  - 01-03
files_modified:
  - packages/cli/src/github/index.ts
  - packages/cli/src/github/sync.ts
  - packages/cli/src/mcp/github-tools.ts
  - packages/cli/src/mcp/board-tools.ts
  - packages/cli/src/mcp/index.ts
autonomous: true
requirements:
  - AC-04
  - AC-07
  - AC-09
  - AC-10
  - AC-11
must_haves:
  truths:
    - "MCP tools for issue lifecycle exist (create, move, comment, close)"
    - "MCP tools for board queries exist (query board, search/filter issues)"
    - "Sync check detects external changes to GitHub issues"
    - "External issue import works through MCP tool"
    - "All existing task-tracking MCP tools replaced with GitHub-backed versions"
  artifacts:
    - path: "packages/cli/src/github/sync.ts"
      provides: "Sync check: detect external changes, verify issue status against code"
      exports: ["syncCheck", "verifyIssueState"]
      min_lines: 80
    - path: "packages/cli/src/github/index.ts"
      provides: "Barrel export for all github modules"
      min_lines: 10
    - path: "packages/cli/src/mcp/github-tools.ts"
      provides: "GitHub issue lifecycle MCP tools replacing phase-tools and todo-tools"
      min_lines: 200
    - path: "packages/cli/src/mcp/board-tools.ts"
      provides: "Board query, search, filter, comment, move MCP tools"
      min_lines: 150
  key_links:
    - from: "packages/cli/src/mcp/github-tools.ts"
      to: "packages/cli/src/github/issues.ts"
      via: "createTaskIssue, createTodoIssue, closeIssue, etc."
      pattern: "import.*from.*github/issues"
    - from: "packages/cli/src/mcp/board-tools.ts"
      to: "packages/cli/src/github/projects.ts"
      via: "moveItemToStatus, getProjectFields, addItemToProject"
      pattern: "import.*from.*github/projects"
    - from: "packages/cli/src/mcp/index.ts"
      to: "packages/cli/src/mcp/github-tools.ts"
      via: "registerGitHubTools in registerAllTools"
      pattern: "registerGitHubTools"
---

<objective>
Build the MCP tool layer that exposes GitHub operations to Claude Code, the sync check mechanism, and wire everything into the MCP server. This replaces existing local-only task tracking with GitHub-backed implementations and adds new board query tools.
</objective>

<execution_context>
Plans 01-03 built the GitHub module layer (gh.ts, types.ts, mapping.ts, projects.ts, labels.ts, milestones.ts, templates.ts, issues.ts). This plan builds the MCP tools on top and creates the sync mechanism.

Key constraint: This is a REPLACEMENT of existing MCP tools, not coexistence [Decision #8]. The existing phase-tools.ts and todo-tools.ts remain but are MODIFIED to also trigger GitHub operations when in 'full' mode. The new github-tools.ts and board-tools.ts add NEW MCP tools.

Key constraint: Graceful degradation [Decision #13, NO-GO]. Every GitHub MCP tool must check `detectGitHubMode()`. In `local-only` mode, fall back to existing local behavior (existing tool functions still work). In `full` mode, perform GitHub operations AND local operations.

AC-09: Sync check before each phase action detects external changes.
AC-10: All existing task-tracking MCP tools replaced with GitHub-backed implementations.
AC-11: New MCP tools: query board, search/filter issues, post comment, move cards.
</execution_context>

<context>
@packages/cli/src/mcp/index.ts (current tool registration — must add new registrations)
@packages/cli/src/mcp/phase-tools.ts (existing phase tools to understand interface)
@packages/cli/src/mcp/todo-tools.ts (existing todo tools to understand interface)
@packages/cli/src/mcp/utils.ts (mcpSuccess, mcpError, detectProjectRoot helpers)
@packages/cli/src/github/types.ts (type definitions)
@packages/cli/src/github/gh.ts (gh CLI wrapper)
@packages/cli/src/github/issues.ts (issue CRUD)
@packages/cli/src/github/projects.ts (project board operations)
@packages/cli/src/github/mapping.ts (mapping persistence)
</context>

<tasks>

<task id="4.1" type="auto">
<files>
packages/cli/src/github/sync.ts
packages/cli/src/github/index.ts
</files>
<action>
Create the sync check module and barrel export.

**sync.ts** exports:

1. **syncCheck(cwd: string): Promise&lt;GhResult&lt;{ inSync: boolean; changes: Array&lt;{ issueNumber: number; field: string; localValue: string; remoteValue: string }&gt; }&gt;&gt;**

   Compare local mapping file against GitHub reality:
   a. Load mapping from `github-issues.json`.
   b. For each tracked issue, run `gh issue view {number} --json state,labels,title` to get current GitHub state.
   c. Compare: if an issue was closed externally (state=closed but local mapping shows "In Progress"), flag it as a change.
   d. Return list of discrepancies.
   e. For efficiency, batch issue lookups: use `gh api graphql` with a single query that fetches multiple issues at once (up to 100 per query). This avoids N+1 sequential calls.

2. **verifyIssueState(issueNumber: number, expectedState: 'open' | 'closed'): Promise&lt;GhResult&lt;{ verified: boolean; actualState: string }&gt;&gt;** — Quick single-issue state check.

3. **handleExternalClose(cwd: string, issueNumber: number): Promise&lt;GhResult&lt;{ action: 'accepted' | 'reopened'; reason: string }&gt;&gt;** — When sync detects an externally closed issue: return info for the AI to decide whether to accept (code actually implements the task) or reopen (code doesn't match). The AI makes the decision based on code verification — this function just provides the data.

**index.ts** — Barrel export file for `packages/cli/src/github/`:

```typescript
export * from './types.js';
export * from './gh.js';
export * from './mapping.js';
export * from './issues.js';
export * from './projects.js';
export * from './labels.js';
export * from './milestones.js';
export * from './templates.js';
export * from './sync.js';
```
</action>
<verify>
<automated>cd /c/Development/cli/maxsim && npx tsc --noEmit packages/cli/src/github/index.ts packages/cli/src/github/sync.ts 2>&1 | head -20</automated>
</verify>
<done>
- sync.ts exports syncCheck, verifyIssueState, handleExternalClose
- syncCheck compares local mapping against GitHub reality
- Batch issue lookups use GraphQL for efficiency (not N sequential calls)
- index.ts re-exports all github modules
- handleExternalClose provides data for AI decision (doesn't auto-decide)
</done>
</task>

<task id="4.2" type="auto">
<files>
packages/cli/src/mcp/github-tools.ts
packages/cli/src/mcp/board-tools.ts
packages/cli/src/mcp/index.ts
</files>
<action>
Create the MCP tool files and wire them into the MCP server.

**github-tools.ts** — `registerGitHubTools(server: McpServer)`:

Follow the EXACT pattern from existing tools (see phase-tools.ts): use `server.tool()`, zod schemas, async handler, try/catch with mcpError, detectProjectRoot(). Every tool checks `detectGitHubMode()` first and degrades gracefully.

Tools to register:

1. **mcp_github_setup** — "Set up GitHub integration: create project board, labels, milestone, and issue templates."
   Params: `{ milestone_title: string }` (optional, defaults to current milestone from STATE.md)
   Action: Call ensureProjectBoard, ensureLabels, ensureMilestone, installIssueTemplates. Store results in mapping file. Return setup summary.
   Degradation: In local-only mode, only install issue templates (those are local files).

2. **mcp_create_plan_issues** — "Create GitHub issues for all tasks in a finalized plan. Creates task issues and parent tracking issue."
   Params: `{ phase: string, plan: string, tasks: array of task objects, milestone?: string }`
   Action: Call createAllPlanIssues. Add all issues to project board. Set estimates. Move all to "To Do" status.
   Degradation: In local-only mode, return success with warning "GitHub not configured, issues not created".

3. **mcp_create_todo_issue** — "Create a GitHub issue for a todo item."
   Params: `{ title: string, description?: string, acceptance_criteria?: string[] }`
   Action: Call createTodoIssue. Add to project. Update mapping.
   Degradation: Fall through to existing local todo creation.

4. **mcp_move_issue** — "Move a GitHub issue to a new status column (To Do, In Progress, In Review, Done)."
   Params: `{ issue_number: number, status: 'To Do' | 'In Progress' | 'In Review' | 'Done' }`
   Action: Load mapping for item_id and field IDs. Call moveItemToStatus. Update local mapping.
   Degradation: In local-only mode, just update local mapping status field.

5. **mcp_close_issue** — "Close a GitHub issue as completed or not planned."
   Params: `{ issue_number: number, reason?: 'completed' | 'not_planned' }`
   Action: Call closeIssue. Move to Done on board. Update mapping.

6. **mcp_post_comment** — "Post a progress comment on a GitHub issue."
   Params: `{ issue_number: number, body: string }`
   Action: Call postComment.

7. **mcp_import_issue** — "Import an external GitHub issue into MAXSIM tracking."
   Params: `{ issue_number: number }`
   Action: Call importExternalIssue. Add maxsim + imported labels. Return issue details for AI to decide placement.

8. **mcp_sync_check** — "Check for external changes to tracked GitHub issues."
   Params: none
   Action: Call syncCheck. Return list of discrepancies.

9. **mcp_supersede_plan** — "Close old plan issues and link to new plan issues."
   Params: `{ phase: string, old_plan: string, new_plan: string }`
   Action: Call supersedePlanIssues.

**board-tools.ts** — `registerBoardTools(server: McpServer)`:

1. **mcp_query_board** — "Query the GitHub project board. Returns all items with their status, estimates, and issue details."
   Params: `{ status?: 'To Do' | 'In Progress' | 'In Review' | 'Done', phase?: string }`
   Action: Use `gh project item-list {num} --owner @me --format json`. Filter by status and/or phase if provided. Return structured list.
   Degradation: In local-only mode, read mapping file and return local status.

2. **mcp_search_issues** — "Search GitHub issues by label, milestone, state, or text query."
   Params: `{ labels?: string[], milestone?: string, state?: 'open' | 'closed' | 'all', query?: string }`
   Action: Use `gh issue list --json number,title,state,labels,milestone --label ... --milestone ... --search ...`. Return filtered results.
   Degradation: In local-only mode, return local mapping data.

3. **mcp_get_issue_detail** — "Get full details of a specific GitHub issue including comments."
   Params: `{ issue_number: number }`
   Action: Use `gh issue view {number} --json number,title,body,state,labels,comments,assignees`.

4. **mcp_set_estimate** — "Set Fibonacci story points on a GitHub issue."
   Params: `{ issue_number: number, points: number }`
   Action: Validate points is in FIBONACCI_POINTS. Load mapping for item_id. Call setEstimate.

**Update index.ts** — Add `registerGitHubTools` and `registerBoardTools` to `registerAllTools`:
```typescript
import { registerGitHubTools } from './github-tools.js';
import { registerBoardTools } from './board-tools.js';
// Add to registerAllTools:
registerGitHubTools(server);
registerBoardTools(server);
```

IMPORTANT: Do NOT remove the existing registerPhaseTools, registerTodoTools, etc. calls. The existing tools continue to work for non-GitHub operations (phase CRUD, state management). The NEW GitHub tools AUGMENT the existing surface. The "replacement" is conceptual — the AI will prefer GitHub tools for task tracking, but existing local tools remain functional.
</action>
<verify>
<automated>cd /c/Development/cli/maxsim && npx tsc --noEmit packages/cli/src/mcp/github-tools.ts packages/cli/src/mcp/board-tools.ts packages/cli/src/mcp/index.ts 2>&1 | head -20</automated>
</verify>
<done>
- github-tools.ts registers 9 MCP tools (setup, create plan issues, create todo, move, close, comment, import, sync, supersede)
- board-tools.ts registers 4 MCP tools (query board, search issues, get detail, set estimate)
- index.ts updated with registerGitHubTools and registerBoardTools
- Every GitHub tool checks detectGitHubMode() and degrades gracefully
- Existing tool registrations (phase, todo, state, context, roadmap, config) NOT removed
- All tools follow the existing pattern: server.tool(), zod schema, async handler, try/catch
- No process.exit(), no stdout writes, no output()/error() imports
</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run `npx tsc --noEmit` from packages/cli to verify full compilation
2. Count MCP tools: `grep -c "server.tool(" packages/cli/src/mcp/*.ts` — should show 13 new tools (9 + 4) plus existing ones
3. Verify graceful degradation: `grep -c "detectGitHubMode\|local-only" packages/cli/src/mcp/github-tools.ts` — should be >= 9 (one per tool)
4. Verify no process.exit: `grep "process.exit" packages/cli/src/mcp/github-tools.ts packages/cli/src/mcp/board-tools.ts` — should be 0
5. Verify existing tools preserved: `grep "registerPhaseTools\|registerTodoTools" packages/cli/src/mcp/index.ts`
</verification>

<success_criteria>
- 13 new MCP tools registered (9 github + 4 board)
- All existing MCP tools preserved (phase, todo, state, context, roadmap, config)
- Every GitHub tool degrades gracefully to local-only mode
- Sync check compares local mapping against GitHub state
- mcp_github_setup creates board, labels, milestone, templates in one call
- mcp_create_plan_issues does eager batch creation (all issues at once)
- Board query tool returns items with status and estimates
- No process.exit() in any MCP tool file
- Full TypeScript compilation passes
</success_criteria>

<output>
Commit message: "feat(github): add MCP tools for GitHub issue lifecycle, board queries, and sync"
Files: packages/cli/src/github/sync.ts, packages/cli/src/github/index.ts, packages/cli/src/mcp/github-tools.ts, packages/cli/src/mcp/board-tools.ts, packages/cli/src/mcp/index.ts
</output>
