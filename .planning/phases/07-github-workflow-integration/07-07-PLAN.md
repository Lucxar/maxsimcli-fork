---
phase: 07-github-workflow-integration
plan: 07
type: execute
wave: 4
depends_on:
  - 07-03
  - 07-04
  - 07-05
  - 07-06
files_modified:
  - templates/agents/executor.md
  - templates/agents/planner.md
  - templates/agents/verifier.md
  - templates/skills/github-artifact-protocol/SKILL.md
  - packages/cli/src/install/shared.ts
  - packages/cli/src/install/hooks.ts
  - templates/workflows/quick.md
autonomous: true
requirements:
  - WIRE-02
  - WIRE-05
user_setup: []
must_haves:
  truths:
    - "Agent prompts reference GitHub Issue reading/writing instead of local file I/O"
    - "github-artifact-protocol skill exists with issue lifecycle, board transitions, comment types"
    - "Skill is in builtInSkills array for installer"
    - "Quick workflow creates GitHub Issues for todos when appropriate"
    - "Installer orphan cleanup includes deprecated workflow names"
  artifacts:
    - path: "templates/agents/executor.md"
      provides: "Instructions to read plan from GitHub, post summary to GitHub, per-task board updates"
    - path: "templates/agents/planner.md"
      provides: "Instructions to post plan to GitHub, create task sub-issues"
    - path: "templates/agents/verifier.md"
      provides: "Instructions to post verification results to GitHub"
    - path: "templates/skills/github-artifact-protocol/SKILL.md"
      provides: "GitHub artifact patterns, issue lifecycle, board transitions, comment type conventions"
    - path: "packages/cli/src/install/shared.ts"
      provides: "github-artifact-protocol in builtInSkills array"
    - path: "packages/cli/src/install/hooks.ts"
      provides: "Deprecated workflow names in orphan cleanup list"
  key_links:
    - from: "templates/agents/executor.md"
      to: "templates/skills/github-artifact-protocol/SKILL.md"
      via: "available_skills reference"
      pattern: "github-artifact-protocol"
    - from: "templates/agents/planner.md"
      to: "templates/skills/github-artifact-protocol/SKILL.md"
      via: "available_skills reference"
      pattern: "github-artifact-protocol"
---

<objective>
Update agent prompts to reference GitHub-based artifact I/O, create the github-artifact-protocol skill, update the installer, and wire remaining workflows (quick.md).
</objective>

<execution_context>
This is the final cleanup plan for Phase 7. Agents (executor, planner, verifier) are generic prompts that get MCP tools injected at spawn time by orchestrator workflows. The agent prompts themselves need minor updates to reference GitHub Issue reading/writing instead of local file I/O. The github-artifact-protocol skill provides shared patterns for all agents.

Locked decisions:
- WIRE-02: No local phase artifact copies
- WIRE-05: Always-live reads from GitHub
- Agent tool injection is orchestrator-mediated (Pattern 3 from research)
</execution_context>

<context>
@templates/agents/executor.md (executor agent prompt)
@templates/agents/planner.md (planner agent prompt)
@templates/agents/verifier.md (verifier agent prompt)
@templates/agents/AGENTS.md (agent system overview)
@packages/cli/src/install/shared.ts (builtInSkills array)
@packages/cli/src/install/hooks.ts (orphan cleanup)
@templates/workflows/quick.md (ad-hoc task workflow)
</context>

<tasks>

<task id="01" type="auto">
  <files>
    - templates/agents/executor.md
    - templates/agents/planner.md
    - templates/agents/verifier.md
  </files>
  <action>
Read all three agent files fully first.

**1. executor.md -- Update I/O references:**

a) In the section describing how the executor receives its plan:
  - Change references from "Read PLAN.md file at {path}" to "Plan content is provided by the orchestrator from a GitHub Issue comment"
  - Remove any @file references to local PLAN.md paths
  - Add: "Plan content comes from the orchestrator which reads it from a GitHub Issue comment posted by mcp_post_plan_comment"

b) In the section describing executor output:
  - Change references from "Write SUMMARY.md to {path}" to "Summary is posted as a GitHub comment by the orchestrator"
  - Add: "After completing tasks, the orchestrator posts a summary comment via mcp_post_comment with type: 'summary'"

c) In the available_skills section, add:
  ```
  | github-artifact-protocol | `.skills/github-artifact-protocol/SKILL.md` | When reading from or writing to GitHub Issues |
  ```

d) Add a note about per-task board transitions:
  - "The orchestrator handles board transitions. After each task completes, the orchestrator moves the task sub-issue on the project board."

**2. planner.md -- Update I/O references:**

a) In output description:
  - Change references from "Write PLAN.md to {path}" to "Plan is posted as a GitHub Issue comment via mcp_post_plan_comment"
  - Add: "Task sub-issues are created via mcp_batch_create_tasks after the plan is posted"

b) In input description:
  - Change references to reading CONTEXT.md/RESEARCH.md from local files to: "Context and research are provided from GitHub Issue comments"

c) Add github-artifact-protocol to available_skills.

**3. verifier.md -- Update I/O references:**

a) In output description:
  - Change references from "Write VERIFICATION.md to {path}" to "Verification results are posted as a GitHub comment via mcp_post_comment with type: 'verification'"

b) Add github-artifact-protocol to available_skills.

**Important:** Do NOT add MCP tools to agent `tools:` frontmatter directly. Tool injection is orchestrator-mediated per the existing architecture.
  </action>
  <verify>
    <automated>cd /c/Development/cli/maxsim && grep -c "github-artifact-protocol\|GitHub Issue comment\|mcp_post" templates/agents/executor.md templates/agents/planner.md templates/agents/verifier.md</automated>
  </verify>
  <done>
- All 3 agent prompts reference GitHub Issue comments for artifact I/O
- All 3 agents list github-artifact-protocol in available_skills
- No references to local PLAN.md, SUMMARY.md, VERIFICATION.md writing remain
- Tool injection remains orchestrator-mediated (no tools in agent frontmatter)
  </done>
</task>

<task id="02" type="auto">
  <files>
    - templates/skills/github-artifact-protocol/SKILL.md
    - packages/cli/src/install/shared.ts
    - packages/cli/src/install/hooks.ts
    - templates/workflows/quick.md
  </files>
  <action>
**1. Create github-artifact-protocol skill:**

Create the directory `templates/skills/github-artifact-protocol/` and write `SKILL.md`:

```markdown
# Skill: GitHub Artifact Protocol

## Trigger
When reading from or writing to GitHub Issues for MAXSIM artifacts.

## Protocol

### Artifact Types and Comment Conventions

All MAXSIM artifacts are stored as GitHub Issue comments with type metadata:

| Artifact | Comment Type | Tool | Format |
|----------|-------------|------|--------|
| Context decisions | `context` | `mcp_post_comment` | `<!-- maxsim:type=context -->` header |
| Research findings | `research` | `mcp_post_comment` | `<!-- maxsim:type=research -->` header |
| Plan content | (plan header) | `mcp_post_plan_comment` | `## Plan NN` header |
| Summary | `summary` | `mcp_post_comment` | `<!-- maxsim:type=summary -->` header |
| Verification | `verification` | `mcp_post_comment` | `<!-- maxsim:type=verification -->` header |
| UAT | `uat` | `mcp_post_comment` | `<!-- maxsim:type=uat -->` header |
| Completion | (structured) | `mcp_post_completion` | Commit SHA + files |

### Issue Lifecycle State Machine

**Phase Issues:**
```
To Do --> In Progress --> In Review --> Done
(created)  (plan done)    (PR created)  (PR merged + verified)
```

**Task Sub-Issues:**
```
To Do --> In Progress --> Done
(created)  (started)      (completed + review passed)
Done --> In Progress (review failed, reopened)
```

### Write Order (WIRE-01)

1. Build content in memory
2. POST to GitHub via MCP tool
3. If successful, operation succeeds
4. If failed, operation aborts entirely -- no partial state

### Rollback Pattern (WIRE-07)

On partial failure during batch operations:
1. Close partially-created issues with `state_reason: 'not_planned'`
2. Post `[MAXSIM-ROLLBACK]` comment explaining why
3. Report what succeeded and what failed
4. Offer targeted retry

### External Edit Detection (WIRE-06)

- Body hash (SHA-256) stored in `github-issues.json` mapping after each write
- On read, compare live body hash against stored hash
- If different, warn user about external modification
- Do not auto-incorporate -- user decides

### What Stays Local

- `config.json` -- project settings
- `PROJECT.md` -- project vision
- `REQUIREMENTS.md` -- requirements
- `ROADMAP.md` -- phase structure
- `STATE.md` -- decisions, blockers, metrics
- `github-issues.json` -- mapping cache (rebuildable)
- `codebase/` -- stack, architecture, conventions
- `todos/` -- pending/completed todos
```

**2. packages/cli/src/install/shared.ts -- Add to builtInSkills:**

Read the file first. Add `'github-artifact-protocol'` to the `builtInSkills` array. Maintain alphabetical order within the array.

**3. packages/cli/src/install/hooks.ts -- Add deprecated workflow names to orphan cleanup:**

Read the file first. If there is an orphan cleanup list of deprecated workflow filenames, check if any old workflow names need to be added. Based on research, plan-phase.md and execute-phase.md might be deprecated in favor of the thin orchestrator pattern. If they are NOT being deprecated (just updated), no orphan entries needed. Only add entries for workflows that are actually being REMOVED in this phase.

Check the research: "Two Workflow Layers" pitfall says both layers exist and "Must update the active layer and deprecate or remove the older detailed workflows." If plan-phase.md and execute-phase.md are being kept (just updated), do NOT add them to orphan cleanup. Only add if actually deleting.

**4. templates/workflows/quick.md -- Better GitHub Issue creation:**

Read the file first. Update quick.md to:
- When creating a todo task, if github_ready is true, also create a GitHub Issue for the todo
- Use `mcp_create_task_issue` or the todo-specific tools
- This is a minor enhancement, not a full rewrite
  </action>
  <verify>
    <automated>cd /c/Development/cli/maxsim && test -f templates/skills/github-artifact-protocol/SKILL.md && echo "Skill exists" && grep "github-artifact-protocol" packages/cli/src/install/shared.ts && echo "In builtInSkills"</automated>
  </verify>
  <done>
- github-artifact-protocol SKILL.md created with full protocol documentation
- Skill covers: artifact types, lifecycle state machine, write order, rollback, external edit detection
- Skill added to builtInSkills array in shared.ts
- Orphan cleanup updated if any workflows are being removed
- quick.md creates GitHub Issues for todos when github_ready is true
  </done>
</task>

</tasks>

<verification>
```bash
# Verify skill exists and is complete
test -f templates/skills/github-artifact-protocol/SKILL.md && echo "Skill file exists"
grep -c "WIRE-01\|WIRE-02\|WIRE-06\|WIRE-07\|WIRE-08" templates/skills/github-artifact-protocol/SKILL.md

# Verify installer includes new skill
grep "github-artifact-protocol" packages/cli/src/install/shared.ts

# Verify agent updates
grep -c "github-artifact-protocol" templates/agents/executor.md templates/agents/planner.md templates/agents/verifier.md

# Build to verify TypeScript
cd /c/Development/cli/maxsim && npm run build:cli
```
</verification>

<success_criteria>
- github-artifact-protocol skill created with comprehensive protocol documentation
- All 3 agents reference the skill and describe GitHub-based artifact I/O
- Skill included in builtInSkills for installer deployment
- quick.md enhanced for GitHub Issue creation
- Full build succeeds
</success_criteria>

<output>
Commit message: `feat(agents): update agent prompts for GitHub I/O, create github-artifact-protocol skill, update installer`
</output>
