---
phase: 01-Infrastructure-Cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/dashboard/ (DELETE entire directory)
  - packages/cli/src/backend/ (DELETE entire directory)
  - packages/cli/src/backend-server.ts (DELETE)
  - packages/cli/src/core/dashboard-launcher.ts (DELETE)
  - packages/cli/src/core/start.ts (DELETE)
  - packages/cli/src/install/dashboard.ts (DELETE)
  - packages/cli/tests/e2e/dashboard.test.ts (DELETE)
  - packages/cli/tests/e2e/dashboard-pty-absent.test.ts (DELETE)
  - package.json
  - packages/cli/package.json
  - packages/cli/tsdown.config.ts
  - packages/cli/scripts/copy-assets.cjs
  - packages/cli/src/cli.ts
  - packages/cli/src/core/index.ts
  - packages/cli/src/install/index.ts
  - packages/cli/tests/e2e/install.test.ts
  - packages/cli/tests/pack.test.ts
  - package-lock.json
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
must_haves:
  truths:
    - "The packages/dashboard/ directory does not exist"
    - "The packages/cli/src/backend/ directory does not exist"
    - "No source file imports from dashboard-launcher, start, backend, or backend-server modules"
    - "npm run build succeeds without errors"
    - "MCP server starts and exposes tools (unaffected by removal)"
    - "express, ws, chokidar, detect-port are not in packages/cli/package.json dependencies"
  artifacts:
    - path: "package.json"
      provides: "Workspace config without dashboard"
      min_lines: 10
    - path: "packages/cli/package.json"
      provides: "CLI dependencies without dashboard deps"
      min_lines: 20
    - path: "packages/cli/tsdown.config.ts"
      provides: "Build config without backend-server entry"
      min_lines: 20
    - path: "packages/cli/src/cli.ts"
      provides: "CLI router without dashboard command"
      min_lines: 100
    - path: "packages/cli/src/core/index.ts"
      provides: "Barrel exports without dashboard-launcher/start"
      min_lines: 100
    - path: "packages/cli/src/install/index.ts"
      provides: "Installer without dashboard copy/subcommand"
      min_lines: 100
  key_links:
    - from: "packages/cli/src/cli.ts"
      to: "packages/cli/src/core/index.ts"
      via: "import from core barrel"
      pattern: "import.*from.*core"
    - from: "packages/cli/src/mcp-server.ts"
      to: "packages/cli/src/mcp/index.ts"
      via: "import registerAllTools"
      pattern: "import.*registerAllTools"
---

<objective>
Remove the dashboard package and backend server entirely from the monorepo, clean all cross-references, and verify the MCP server remains fully functional. After this plan, the codebase has no dashboard or backend server code, no stale dependencies, and builds cleanly.
</objective>

<execution_context>
This is the first plan of the Infrastructure Cleanup phase. The dashboard (Vite+React frontend + Express backend) is being replaced by GitHub Project Board. Research confirmed the backend server and MCP server are FULLY SEPARATE -- they share no initialization, no code paths, and no state. The MCP server uses stdio transport via `@modelcontextprotocol/sdk` and is completely independent.

Key decisions from CONTEXT.md (locked, non-negotiable):
- Aggressive full removal -- no traces anywhere
- Delete `packages/dashboard/` entirely
- Remove ALL dashboard references from build pipeline, installer, CLI router, tests
- Remove `@maxsim/core` path alias (only dashboard used it)
- MCP server: leave as-is in Phase 1, do NOT modify MCP server tools
- Backend server: fully independent from MCP server, delete entirely
</execution_context>

<context>
@file .planning/phases/01-Infrastructure-Cleanup/01-RESEARCH.md (sections: Dashboard Removal Blast Radius, Backend Server vs MCP Server Independence, Dependencies to REMOVE/KEEP, Workspace Configuration Changes, Common Pitfalls 1-4)
@file .planning/phases/01-Infrastructure-Cleanup/01-CONTEXT.md (sections: Dashboard Removal Boundaries, MCP Server Scope)
@file .planning/CONVENTIONS.md
@file .planning/NO-GOS.md
</context>

<tasks>

<task id="01-01-T1" type="auto">
<title>Delete dashboard and backend server files, clean all references</title>
<files>
- packages/dashboard/ (DELETE entire directory tree)
- packages/cli/src/backend/ (DELETE entire directory: server.ts, lifecycle.ts, terminal.ts, types.ts, index.ts)
- packages/cli/src/backend-server.ts (DELETE)
- packages/cli/src/core/dashboard-launcher.ts (DELETE)
- packages/cli/src/core/start.ts (DELETE)
- packages/cli/src/install/dashboard.ts (DELETE)
- packages/cli/tests/e2e/dashboard.test.ts (DELETE)
- packages/cli/tests/e2e/dashboard-pty-absent.test.ts (DELETE)
- package.json (MODIFY: remove packages/dashboard from workspaces, remove build:dashboard script, update build script to just `npm run build:cli`)
- packages/cli/tsdown.config.ts (MODIFY: remove backend-server entry block at lines ~41-46)
- packages/cli/scripts/copy-assets.cjs (MODIFY: remove dashboard copy step)
- packages/cli/src/cli.ts (MODIFY: remove dashboard command handler ~lines 443, 527-607, remove related imports)
- packages/cli/src/core/index.ts (MODIFY: remove dashboard-launcher and start re-exports ~lines 290-308)
- packages/cli/src/install/index.ts (MODIFY: remove dashboard import, remove dashboard copy section ~lines 343-368, remove dashboard subcommand ~lines 534-536)
- packages/cli/tests/e2e/install.test.ts (MODIFY: remove dashboard test case ~lines 116-125)
- packages/cli/tests/pack.test.ts (MODIFY: remove any assertions checking for backend-server.cjs in dist/)
</files>
<action>
Execute the following in order:

1. **Delete entire directories and files:**
   - `rm -rf packages/dashboard/`
   - `rm -rf packages/cli/src/backend/`
   - `rm packages/cli/src/backend-server.ts`
   - `rm packages/cli/src/core/dashboard-launcher.ts`
   - `rm packages/cli/src/core/start.ts`
   - `rm packages/cli/src/install/dashboard.ts`
   - `rm packages/cli/tests/e2e/dashboard.test.ts`
   - `rm packages/cli/tests/e2e/dashboard-pty-absent.test.ts`

2. **Update root package.json:**
   - Remove `"packages/dashboard"` from `workspaces` array (keep `packages/cli` and `packages/website`)
   - Remove `"build:dashboard"` script entirely
   - Change `"build"` script from `"npm run build:dashboard && npm run build:cli"` to `"npm run build:cli"`
   - Remove the OOM workaround from `build:cli`: change from `"cd packages/cli && npx cross-env NODE_OPTIONS=--max-old-space-size=8192 npx tsdown && node scripts/copy-assets.cjs"` to `"cd packages/cli && npx tsdown && node scripts/copy-assets.cjs"`
   - Note: cross-env is a devDependency used only for the OOM workaround. If no other script uses it, remove `cross-env` from devDependencies in packages/cli/package.json too.

3. **Update packages/cli/package.json:**
   - Remove from `dependencies`: `express`, `ws`, `chokidar`, `detect-port`
   - Remove from `devDependencies`: `@types/express`, `@types/ws`, `cross-env` (if only used by OOM workaround)
   - Do NOT remove: `@modelcontextprotocol/sdk`, `zod`, `simple-git`, `slugify`, `yaml`, `escape-string-regexp`, `chalk`, `ora`, `@inquirer/prompts`, `minimist`, `fs-extra`, `figlet`

4. **Update packages/cli/tsdown.config.ts:**
   - Remove the entire backend-server entry block (the object with `entry: { 'backend-server': 'src/backend-server.ts' }` and its noExternal/external overrides)
   - Keep all other entries (install, cli, mcp-server, hooks) unchanged

5. **Update packages/cli/scripts/copy-assets.cjs:**
   - Find and remove the step that copies dashboard build output to dist/assets/dashboard/
   - Keep all other copy steps (templates, hooks, changelog, README)

6. **Update packages/cli/src/cli.ts:**
   - Remove the `dashboard` command case from the command switch/dispatch
   - Remove any imports referencing dashboard-launcher, start, or backend modules
   - Do NOT modify any other command handlers

7. **Update packages/cli/src/core/index.ts:**
   - Remove re-exports of `dashboard-launcher` module (~line with `export * from './dashboard-launcher.js'`)
   - Remove re-exports of `start` module (~line with `export * from './start.js'`)
   - Keep all other re-exports intact

8. **Update packages/cli/src/install/index.ts:**
   - Remove `import` of dashboard module (`./dashboard.js` or similar)
   - Remove the section that copies dashboard files (~lines 343-368)
   - Remove the dashboard subcommand handler (~lines 534-536)
   - Keep all other install logic intact

9. **Update test files:**
   - `packages/cli/tests/e2e/install.test.ts`: Remove the dashboard-specific test case (~lines 116-125)
   - `packages/cli/tests/pack.test.ts`: If it asserts `backend-server.cjs` exists in dist/, remove that assertion. If it asserts `dashboard/` exists in dist/assets/, remove that assertion. Keep all other pack assertions.

10. **Run TypeScript check to find any remaining broken references:**
    - `cd packages/cli && npx tsc --noEmit`
    - Fix any errors related to missing imports from deleted modules
    - Common: if any file still imports from `./backend/`, `./dashboard-launcher`, `./start`, or `./install/dashboard`, remove those imports
</action>
<verify>
<automated>cd C:/Development/cli/maxsim && test ! -d packages/dashboard && test ! -d packages/cli/src/backend && test ! -f packages/cli/src/backend-server.ts && test ! -f packages/cli/src/core/dashboard-launcher.ts && test ! -f packages/cli/src/core/start.ts && test ! -f packages/cli/src/install/dashboard.ts && echo "DELETED OK" && cd packages/cli && npx tsc --noEmit 2>&1 | tail -5 && echo "TSC OK"</automated>
</verify>
<done>
- packages/dashboard/ directory does not exist
- packages/cli/src/backend/ directory does not exist
- backend-server.ts, dashboard-launcher.ts, start.ts, install/dashboard.ts are all deleted
- Dashboard test files are deleted
- No TypeScript compilation errors (tsc --noEmit succeeds)
- No source file contains imports referencing deleted modules
</done>
</task>

<task id="01-01-T2" type="auto">
<title>Regenerate lockfile, verify build succeeds, verify MCP server starts</title>
<files>
- package-lock.json (REGENERATE via npm install)
</files>
<action>
1. **Regenerate lockfile:**
   - Run `npm install` from the repo root to regenerate `package-lock.json` with the updated workspace config (no dashboard) and removed dependencies
   - This resolves Pitfall 1 (stale lockfile entries for dashboard-only packages like React, xterm)

2. **Verify clean build:**
   - Run `npm run build` from the repo root
   - Confirm it completes without errors and WITHOUT the OOM workaround (no NODE_OPTIONS=--max-old-space-size=8192)
   - Confirm dist/ is produced with: cli.cjs, install.cjs, mcp-server.cjs (but NOT backend-server.cjs)
   - Confirm dist/assets/hooks/ contains: maxsim-check-update.cjs, maxsim-context-monitor.cjs, maxsim-statusline.cjs

3. **Verify tests pass:**
   - Run `npm test` (unit tests)
   - Run `npm run e2e` (e2e tests -- should pass with dashboard tests removed)

4. **Verify MCP server is unaffected (INFRA-03):**
   - Run `node packages/cli/dist/mcp-server.cjs --help` or attempt to start it and confirm it initializes without errors
   - The MCP server should start (it uses stdio transport, no Express dependency)
   - Kill the process after confirming startup

5. **Verify no dashboard references remain in source:**
   - Run `grep -r "dashboard" packages/cli/src/ --include="*.ts" -l` and confirm zero results (or only comments/strings that are not import/require statements)
   - Run `grep -r "backend-server" packages/cli/src/ --include="*.ts" -l` and confirm zero results
</action>
<verify>
<automated>cd C:/Development/cli/maxsim && npm run build 2>&1 | tail -3 && echo "BUILD OK" && npm test 2>&1 | tail -5 && echo "TEST OK" && test ! -f packages/cli/dist/backend-server.cjs && echo "NO BACKEND SERVER IN DIST" && test -f packages/cli/dist/mcp-server.cjs && echo "MCP SERVER EXISTS"</automated>
</verify>
<done>
- npm install completes and package-lock.json is regenerated
- npm run build succeeds without OOM workaround
- dist/ contains cli.cjs, install.cjs, mcp-server.cjs but NOT backend-server.cjs
- npm test passes (unit tests)
- MCP server binary exists and can initialize
- No source files import from deleted dashboard/backend modules
- express, ws, chokidar, detect-port are absent from packages/cli/package.json
</done>
</task>

</tasks>

<verification>
Run the full verification sequence after both tasks complete:

```bash
# 1. Confirm deletions
test ! -d packages/dashboard && echo "dashboard gone"
test ! -d packages/cli/src/backend && echo "backend gone"

# 2. Confirm no stale references
grep -r "dashboard-launcher\|backend-server\|/backend/" packages/cli/src/ --include="*.ts" -l | wc -l  # expect 0

# 3. Build
npm run build  # must succeed without OOM workaround

# 4. Tests
npm test  # unit tests pass

# 5. Dist contents
ls packages/cli/dist/*.cjs  # should show cli.cjs, install.cjs, mcp-server.cjs (no backend-server.cjs)

# 6. MCP server starts
timeout 5 node packages/cli/dist/mcp-server.cjs 2>&1 || true  # should show MCP initialization, not crash

# 7. Dependencies cleaned
grep -c "express\|\"ws\"\|chokidar\|detect-port" packages/cli/package.json  # expect 0
```
</verification>

<success_criteria>
- INFRA-01: Dashboard package is completely removed -- no directory, no references, no dependencies
- INFRA-02: Backend server is completely removed -- no source, no build entry, no dist artifact
- INFRA-03: MCP server is untouched and starts successfully after dashboard/backend removal
- Build succeeds without OOM workaround (NODE_OPTIONS removed)
- All unit tests pass
- No stale imports or references to deleted modules anywhere in source
</success_criteria>

<output>
Commit: `fix(infra): remove dashboard package and backend server [INFRA-01, INFRA-02, INFRA-03]`
Artifacts: Updated package.json, tsdown.config.ts, cli.ts, install/index.ts, core/index.ts, copy-assets.cjs, regenerated package-lock.json
</output>
